# Assets needed

Drop your real files in here with these exact names (or update the paths in `index.html`):

- `assets/video/swiss-mountains.mp4` — looping Swiss mountain footage for the hero background. Keep it short (8–15s), muted-friendly, and under ~8MB for fast load (compress with Handbrake/ffmpeg if needed).
- `assets/img/bottle.png` — the ZÉLL-V bottle product shot, transparent background, from the photo you shared.
- `assets/img/box.png` — the black ZÉLL-V packaging box, transparent background.

Until these are added, the page still works: the hero falls back to the dark gradient background, and the product section will just show broken image icons until `bottle.png` / `box.png` are added.
